% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.bal.tab.R
\name{extract.bal.tab}
\alias{extract.bal.tab}
\alias{as.data.frame.bal.tab}
\alias{format.bal.tab}
\title{Extract a Balance Table for Further Use}
\usage{
\method{as.data.frame}{bal.tab}(x, row.names = NULL, optional = FALSE, ..., wide = FALSE)

\method{format}{bal.tab}(
  x,
  ...,
  digits = max(3L, getOption("digits") - 3L),
  component = "balance"
)
}
\arguments{
\item{x}{a \code{bal.tab} object; the output of a call to \code{\link[=bal.tab]{bal.tab()}}.}

\item{row.names, optional}{ignored; present for consistency with the \code{\link[=as.data.frame]{as.data.frame()}} generic.}

\item{...}{arguments passed to \code{\link[=print.bal.tab]{print.bal.tab()}} to control which statistics, samples, and covariates are included, e.g. \code{stats}, \code{disp}, \code{un}, \code{imbalanced.only}, or \code{disp.thresholds}. The \code{.all} and \code{.none} shorthands are accepted as they are by \code{print()}. Arguments that were not computed in the original call to \code{bal.tab()} cannot be requested here, for the same reason they cannot be requested in \code{print()}.}

\item{wide}{\code{logical}; for \code{as.data.frame()}, whether to return the table in the layout \code{print()} uses, with one column per sample and statistic, rather than the default tidy layout. Default is \code{FALSE}.}

\item{digits}{for \code{format()}, the number of significant digits to display. Default is the same as for \code{print()}.}

\item{component}{for \code{format()}, which table to return: \code{"balance"} (the default) for the balance table, or \code{"observations"} for the sample size table.}
}
\value{
\code{as.data.frame()} returns a data frame with one row per covariate, sample, and statistic, and these columns:
\describe{
\item{\code{variable}}{the covariate name, as it appears in the row names of the balance table.}
\item{\code{type}}{the covariate type: \code{"Binary"}, \code{"Contin."}, or \code{"Distance"}.}
\item{\code{sample}}{\code{"Unadjusted"}, or the name of the set of weights.}
\item{\code{stat}}{the name of the statistic, using the same names as \code{bal.tab()}'s \code{stats} argument (e.g., \code{"mean.diffs"}), with \code{"mean"} and \code{"sd"} for the distribution summary statistics.}
\item{\code{group}}{for \code{"mean"} and \code{"sd"}, the name of the treatment group the value describes, as \code{\link[=bal.tab]{bal.tab()}} names it: the two group names for a binary treatment, the level for a multi-category one, \code{"Uncensored"} or \code{"Full"} for a censoring indicator, and \code{"All"} for a continuous treatment, which has no groups, or for the full sample when \code{pairwise = FALSE}. \code{NA} for a statistic that contrasts two groups, which belongs to neither of them.}
\item{\code{estimate}}{the value of the statistic.}
\item{\code{threshold}}{the balance verdict, e.g. \code{"Balanced, <0.1"}, when a threshold was requested for that statistic; \code{NA} otherwise.}
\item{\code{threshold.value}}{the numeric threshold; \code{NA} when none was requested.}
}
When the data are segmented -- by cluster, imputation, treatment pair, time point, or subclass -- one further column per level of segmentation identifies it. Segmentation is always a column, never a nested list, so the result is a single rectangle whatever the shape of the input.

A multi-category treatment is reported one pair of groups at a time, but a mean or a standard deviation belongs to a group rather than to a comparison, and is the same in every pair that group appears in. Such a row therefore appears once, with \code{pair} set to \code{NA}; only the statistics that contrast two groups carry a \code{pair}. The same applies to the full sample's own means when \code{pairwise = FALSE}, which would otherwise be repeated against every group.

With \code{wide = TRUE}, the columns are those \code{print()} displays, with the covariate names moved from the row names into a \code{variable} column and any segmentation columns retained.

\code{format()} returns a data frame of character vectors with the covariate names as row names, formatted exactly as \code{print()} displays them: rounded to \code{digits}, padded to a common number of decimal places, with \code{NA} shown as \code{"."}.
}
\description{
\code{as.data.frame()} extracts the balance statistics computed by \code{\link[=bal.tab]{bal.tab()}} as a tidy data frame, one row per covariate, sample, and statistic. \code{format()} returns the balance table exactly as \code{\link[=print.bal.tab]{print.bal.tab()}} displays it, as a data frame of formatted strings, ready to be passed to a table-rendering function.

Together they are meant to remove the need to pick apart a \code{bal.tab} object by hand when reporting balance in a document.
}
\details{
Both functions accept every argument \code{print()} accepts, and resolve them the same way, so the same warnings are raised when a requested value was not computed because \code{quick = TRUE} in the original call to \code{bal.tab()}.

\code{as.data.frame()} returns the balance statistics themselves, from each innermost balance table. It does not return the summaries across clusters, imputations, treatment pairs, or time points, which are aggregates of those statistics; \code{format()} returns the summary when that is what \code{print()} displays.
}
\examples{
data("lalonde", package = "cobalt")

b <- bal.tab(treat ~ age + educ + race + re74, data = lalonde,
             s.d.denom = "pooled", stats = c("m", "ks"),
             thresholds = c(m = .1), un = TRUE)

#Tidy: one row per covariate, sample, and statistic
head(as.data.frame(b))

#The layout print() shows
as.data.frame(b, wide = TRUE)

#Ready for knitr::kable() or any other table renderer
format(b)

format(b, component = "observations")

#print()'s arguments work here too
as.data.frame(b, stats = "ks", un = FALSE)

}
\seealso{
\itemize{
\item \code{\link[=bal.tab]{bal.tab()}}
\item \code{\link[=print.bal.tab]{print.bal.tab()}}
\item \code{\link[=love.plot]{love.plot()}} for a graphical alternative
}
}
